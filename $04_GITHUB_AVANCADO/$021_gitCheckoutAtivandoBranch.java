package $04_GITHUB_AVANCADO;

public class $021_gitCheckoutAtivandoBranch {
	/*
	 * GIT CHECKOUT
	 *   E o comando utilizado para receber branchs do servidor e tambem para mudar de branch local
	 *   
	 *   git checkout -b + (nome da branch) -> cria a branch e a coloca como ativa.
	 *   
	 *   git checkout + (nome da branch ) ->  muda a branch ativa para a qual voce chamou pelo nome.
	 *   
	 *   caso tenha uma branch que esteja no github e voce queira usar porque ela não estra em se repositorio local
	 *   voce primeiro da um git fetchh,depois um git checkout + (nome da branch) isso faz voce puchar essa branch para seu ambiente 
	 *   local.
	 *   
	 *   git fetch
	 *   git checkout logof
	 *   
	 *   
	 */
}
