package $04_GITHUB_AVANCADO;

public class $014_GitPushEnviandoAoServidor {
	/*
	 * GIT PUSH
	 *   E o comando que envia para o github seus dados alterados no git local.
	 *   
	 *   Voce pode usar o git status antes para ver a situacao do seu projeto,caso tenha que adicionar dados e comitar
	 *   algo o git ira lhe pedir isso antes de voce fazer um push..
	 *   
	 *   voce pode tambem utilizar um git remote -v para ver se a conecao do seu repositorio local esta correta com o re
	 *   positorio do github.
	 *   
	 *   coso voce precise conectar seu repositorio existente com o repositorio do github voce usa o comando git remote add + a url do repositorio
	 *   
	 *   por fim usa-se o git push para enviar os dados comitados para o repositorio do github.
	 *   
	 *   RESUMO
	 *      GIT STATUS -> VERIFICA A SITUACAO DO PROJETO
	 *      GIT ADD . -> ADICIONA TODAS AS ALTERACOES DO PROJETO
	 *      GIT COMMIT -M " " -> DESCREVE O PORQUE DESSE COMMIT
	 *      GIT PUSH ORIGIN  + NOME DA BRANCH -> ENVIA AS ALTERACOES PARA O GITHUB. 
	 */
}
